# Optional overrides for notebook defaults. Delete or edit as needed.
smoke_mode: true
reference_dir: Reference
data_dir: data
reports_dir: reports
time_knots: [60, 120, 300]
late_flags: [120, 60]
p_clip_min: 0.05
p_clip_max: 0.98
kickable_cap_modeling: 65
kickable_cap_audit: 50
tau_grid: [0.03, 0.05, 0.07, 0.10]
df_distance: 5
df_yardline: 5
df_yards_to_go: 5
# Additional stage-specific toggles can be added below; notebooks will
# pick them up if names match the in-notebook defaults.
control_function_on: true
